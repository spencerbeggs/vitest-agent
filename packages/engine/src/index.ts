/**
 * @vitest-agent/engine
 *
 * Scaffold entry point for the platform-dependent half of the former
 * `@vitest-agent/sdk` (issue #412): services, layers, the SQLite data
 * layer, migrations, and platform resolution. Populated by a later task.
 *
 * @packageDocumentation
 */

export { CURRENT_ENGINE_VERSION } from "./version.js";
