{
	"name": "vitest-agent-sidecar-darwin-x64",
	"version": "0.0.0",
	"private": true,
	"description": "darwin-x64 prebuilt binary for vitest-agent-sidecar.",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/spencerbeggs/vitest-agent.git",
		"directory": "packages/sidecar-darwin-x64"
	},
	"license": "MIT",
	"author": {
		"name": "C. Spencer Beggs",
		"email": "spencer@beggs.codes",
		"url": "https://spencerbeg.gs"
	},
	"files": ["bin/vitest-agent-sidecar"],
	"scripts": {
		"build:dev": "exit 0",
		"build:prod": "tsdown --config tsdown.config.ts",
		"types:check": "exit 0"
	},
	"dependencies": {
		"vitest-agent-cli": "workspace:*",
		"vitest-agent-sdk": "workspace:*"
	},
	"devDependencies": {
		"@tsdown/exe": "^0.22.0",
		"tsdown": "^0.22.0"
	},
	"os": ["darwin"],
	"cpu": ["x64"]
}
