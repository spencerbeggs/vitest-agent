# @vitest-agent/sidecar-linux-arm64

Prebuilt `linux-arm64` binary for [@vitest-agent/sidecar](https://github.com/spencerbeggs/vitest-agent). Installed automatically as an optional dependency on matching platforms; do not depend on it directly.
