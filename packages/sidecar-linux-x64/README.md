# @vitest-agent/sidecar-linux-x64

Prebuilt `linux-x64` binary for [@vitest-agent/sidecar](https://github.com/spencerbeggs/vitest-agent). Installed automatically as an optional dependency on matching platforms; do not depend on it directly.
