---
"@vitest-agent/sidecar": major
---

## Features

Initial stable release. Node Single Executable Application binary for the per-Bash-call `inject-env` hot path. Prebuilt per-platform binaries ship as optional dependencies.
