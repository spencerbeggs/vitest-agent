---
"@vitest-agent/mcp": major
---

## Features

Initial stable release. The `vitest-agent-mcp` server exposes the test landscape and TDD workflow to agents through an action-keyed tRPC tool surface, plus MCP resources and framing prompts.
